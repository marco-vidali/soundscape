import FavoriteColorCard from "@/features/color/FavoriteColorCard";
import FullScreenCenter from "@/ui/atoms/FullScreenCenter";

const FavoriteColor = () => {
    return (
        <FullScreenCenter>
            <FavoriteColorCard />
        </FullScreenCenter>
    );
};

export default FavoriteColor;
